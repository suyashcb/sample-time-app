[tool.ruff]
line-length = 100
preview = true
target-version = "py314"

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
]

[tool.ruff.lint.per-file-ignores]
"tests/test_app.py" = ["D"]

[tool.pytest.ini_options]
testpaths = ["tests"]
